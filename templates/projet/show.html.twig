{% extends 'base.html.twig' %}

{% block title %}Hello ProjetController!{% endblock %}

{% block page_content %}
    <div id="tasks-container">
        {% for statut in statuts %}
            <div class="task-column">
                <h2>{{ statut.libelle }}</h2>
                {%  for tache in tachesParStatut[statut.id] %}
                    <div class="tasks-list">
                        <a class="task" href="tache.html">
                            <h3>{{ tache.nom }}</h3>
                            <p>{{ tache.description }}</p>
                            <div class="task-infos">
                                <div class="task-deadline">
                                    <i class="fa-regular fa-calendar"></i> {{ tache.dateDeadline|date('d M Y') }}
                                </div>
                            </div>
                        </a>
                    </div>
                {% endfor %}
                <a href="tache-add.html" class="task-add">
                    + Ajouter une tâche
                </a>
            </div>
        {%  endfor %}
    </div>
{% endblock %}
